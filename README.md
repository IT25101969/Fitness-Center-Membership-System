# Fitness-Center-Membership-System
Fitness Center Membership System: Java Servlet/JSP web app to manage members with CRUD (register, search, update, delete). Demonstrates OOP (encapsulation, inheritance, polymorphism) and supports file-based storage plus optional MySQL database. Built in IntelliJ IDEA.
